const createEvent = require("./handlers/createEvent");
const getEvents = require("./handlers/getEvents");
const getSingleEvent = require("./handlers/getSingleEvent");
const joinEvent = require("./handlers/joinEvent");

module.exports = {
  createEvent,
  getEvents,
  getSingleEvent,
  joinEvent,
};
